<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="#1A1209">

    <!-- Header -->
    <TextView
        android:id="@+id/tvMapTitle"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:text="🗺️  BẢN ĐỒ PHIÊU LƯU"
        android:textSize="18sp"
        android:textStyle="bold"
        android:textColor="#FFD700"
        android:gravity="center"
        android:padding="12dp"
        android:background="#2D2010"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"/>

    <!-- MapView tùy chỉnh: vẽ bản đồ và đường đi -->
    <com.example.mymap2.MapView
        android:id="@+id/mapView"
        android:layout_width="0dp"
        android:layout_height="0dp"
        app:layout_constraintTop_toBottomOf="@id/tvMapTitle"
        app:layout_constraintBottom_toTopOf="@id/infoPanel"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"/>

    <!-- Panel thông tin -->
    <RelativeLayout
        android:id="@+id/infoPanel"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:background="#2D2010"
        android:padding="12dp"
        app:layout_constraintBottom_toTopOf="@id/btnRow"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent">

        <TextView
            android:id="@+id/tvCurrentLocation"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="📍 Vị trí: Làng"
            android:textColor="#F0E6C8"
            android:textSize="13sp"
            android:layout_alignParentStart="true"/>

        <TextView
            android:id="@+id/tvDistance"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="📏 --"
            android:textColor="#8A7A5A"
            android:textSize="13sp"
            android:layout_alignParentEnd="true"/>

        <TextView
            android:id="@+id/tvDestination"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="🎯 Đích: Chưa chọn"
            android:textColor="#FFD700"
            android:textSize="13sp"
            android:layout_below="@id/tvCurrentLocation"
            android:layout_marginTop="4dp"/>

    </RelativeLayout>

    <!-- Hàng nút dưới cùng -->
    <LinearLayout
        android:id="@+id/btnRow"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:gravity="center"
        android:padding="10dp"
        android:background="#1A1209"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent">

        <Button
            android:id="@+id/btnBack"
            android:layout_width="0dp"
            android:layout_height="48dp"
            android:layout_weight="1"
            android:text="◀ Về"
            android:textColor="#F0E6C8"
            android:backgroundTint="#2D2010"
            android:layout_marginEnd="8dp"
            android:stateListAnimator="@null"/>

        <Button
            android:id="@+id/btnRoute"
            android:layout_width="0dp"
            android:layout_height="48dp"
            android:layout_weight="2"
            android:text="🧭  Chỉ Đường!"
            android:textSize="14sp"
            android:textStyle="bold"
            android:textColor="#1A1209"
            android:backgroundTint="#FFD700"
            android:stateListAnimator="@null"/>

    </LinearLayout>

</androidx.constraintlayout.widget.ConstraintLayout>